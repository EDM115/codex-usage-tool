{
  "name": "codex-usage-tool",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Generate self-contained Codex usage reports from local .codex files and dashboard APIs",
  "scripts": {
    "usage": "bun src/cli.ts",
    "test": "bun test",
    "check": "bun --syntax-only src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/bun": "^1.3.0"
  }
}
