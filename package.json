{
  "name": "codex-usage-tool",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Generate Codex token usage heatmaps, charts, HTML reports, and cost estimates",
  "scripts": {
    "usage": "bun src/cli.ts",
    "test": "bun test",
    "check": "bun --syntax-only src/cli.ts"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2"
  },
  "devDependencies": {}
}
