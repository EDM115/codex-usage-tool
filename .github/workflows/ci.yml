name: CI

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  ci:
    name: "Checks types & tests"
    runs-on: ubuntu-slim
    timeout-minutes: 10

    steps:
      - name: "Checkout"
        uses: actions/checkout@v7

      - name: "Install Bun"
        uses: oven-sh/setup-bun@v2

      - name: "Install dependencies"
        run: bun i --frozen-lockfile

      - name: "Types check"
        run: bun typecheck

      - name: "Run the tests"
        run: bun test
